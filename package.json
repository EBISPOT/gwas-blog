{
  "name": "gwas-blog",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "gh-pages -d dist --dotfiles",
    "deploy2": "push-dir --dir=dist --branch=gh-pages"
  },
  "dependencies": {
    "@nuxt/content": "^1.9.0",
    "core-js": "^3.15.1",
    "nuxt": "^2.15.7"
  },
  "devDependencies": {
    "gh-pages": "^4.0.0",
    "push-dir": "^0.4.1"
  }
}
